document.getElementById('hapusbtn').addEventListener('click', function() {
            const inputs = document.querySelectorAll('.row input, .row select');
            inputs.forEach(input => {
                input.value = '';
            });
        });

document.getElementById('hapusbtn2').addEventListener('click', function() {
            const inputs = document.querySelectorAll('.row input, .row select');
            inputs.forEach(input => {
                input.value = '';
            });
        });
document.getElementById('hapusbtn3').addEventListener('click', function() {
            const inputs = document.querySelectorAll('.row input, .row select');
            inputs.forEach(input => {
                input.value = '';
            });
        });
document.getElementById('hapusbtn4').addEventListener('click', function() {
            const inputs = document.querySelectorAll('.row input, .row select');
            inputs.forEach(input => {
                input.value = '';
            });
        });    
document.getElementById('hapusbtn5').addEventListener('click', function() {
            const inputs = document.querySelectorAll('.row input, .row select');
            inputs.forEach(input => {
                input.value = '';
            });
        });
document.getElementById('hapusbtn6').addEventListener('click', function() {
            const inputs = document.querySelectorAll('.row input, .row select');
            inputs.forEach(input => {
                input.value = '';
            });
        });